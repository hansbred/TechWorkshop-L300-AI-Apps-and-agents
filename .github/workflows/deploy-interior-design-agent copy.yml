name: Run Interior Design Agent Initializer

on:
  push:
    paths:
      - 'src/app/agents/interiorDesignAgent_initializer.py'
      - 'src/prompts/InteriorDesignAgentPrompt.txt'
      - '.github/workflows/deploy-interior-design-agent.yml'
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  run-interior-design-agent:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create .env file from secret
        run: echo "${{ secrets.ENV }}" > src/.env

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r src/requirements.txt
          pip install azure-ai-projects

      - name: Run interiorDesignAgent_initializer.py
        run: python src/app/agents/interiorDesignAgent_initializer.py
        env:
          ENV_FILE: src/.env
